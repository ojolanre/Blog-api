<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %></title>
</head>
<body>
    <h1><%= blog.title %></h1>
   <p>Author: <%= blog.author.first_name %> <%= blog.author.last_name %> </p>
    <p><%= blog.description %></p>
    <p>Tags: <%= blog.tags.join(', ') %></p>
    <p><%= blog.body %></p>
    <p>State: <%= blog.state %></p>
    <a href="/blogs/user">My Page</a>
    <form action="/blogs/<%= blog._id %>" method="put" style="display: inline;">
        <input type="hidden" name="state" value="published">
        <button type="submit">Publish</button>
    </form>
    
</body>
</html>